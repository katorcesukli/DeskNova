services:
  mysql:
    container_name: 'desk-nova-mysql'
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=desk_nova_db'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=secret'
      - 'MYSQL_USER=user'
    ports:
      - '3307:3306'