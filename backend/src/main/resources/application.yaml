

spring:
  application:
    name: desk-nova

  datasource:
    url: jdbc:mysql://localhost:3306/desk_nova?useSSL=false&serverTimezone=UTC
    username: root
    password: B0ch0g
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    baseline-on-migrate: true

